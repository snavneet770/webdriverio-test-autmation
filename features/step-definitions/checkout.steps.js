const { Given, When, Then } = require('@wdio/cucumber-framework');
const { expect, $ } = require('@wdio/globals')
const categoryPage = require('../pageobjects/checkout.page');


Then(/^the checkout page is open$/, async() => {
    
	
});